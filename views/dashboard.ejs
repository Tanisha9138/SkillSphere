<!-- <!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Dashboard</title>
  <link rel="stylesheet" href="/style1.css" />
  <style>
    body {
      margin: 0;
      padding: 0;
      font-family: 'Poppins', sans-serif;
      background-color: #0f0f0f;
      color: white;
      overflow: hidden;
    }

    .container {
      display: flex;
      justify-content: space-between;
      align-items: center;
      height: 100vh;
      padding: 0 5%;
      position: relative;
    }

    .dashboard-section {
      flex: 1;
      margin-right: 40px;
    }

    .welcome-section h1 {
      font-size: 3em;
    }

    .welcome-section span {
      color: white;
    }

    .dashboard-card {
      background: rgba(255, 255, 255, 0.05);
      backdrop-filter: blur(10px);
      border-radius: 20px;
      padding: 40px 30px;
      width: 350px;
      box-shadow: 0 0 20px rgba(0, 0, 0, 0.5);
      position: relative;
      z-index: 1;
    }

    .dashboard-card h2 {
      margin-bottom: 5px;
    }

    .dashboard-card p {
      margin-bottom: 20px;
      font-size: 0.9em;
      color: #aaa;
    }

    .dashboard-list {
      list-style: none;
      padding: 0;
      margin-bottom: 20px;
    }

    .dashboard-list li {
      margin-bottom: 10px;
    }

    .dashboard-btn, .dashboard-link {
      display: inline-block;
      margin: 8px 8px 0 0;
      padding: 10px 18px;
      border-radius: 8px;
      border: none;
      background: linear-gradient(to right, #5f2c82, #49a09d);
      color: white;
      text-decoration: none;
      font-weight: bold;
      transition: box-shadow 0.2s;
      cursor: pointer;
    }

    .dashboard-btn.logout-btn {
      background: #c33764;
      margin-top: 24px;
    }

    .dashboard-btn:hover, .dashboard-link:hover {
      box-shadow: 0 3px 12px rgba(0,114,255,0.3);
    }

    .bg-circle {
      position: absolute;
      width: 300px;
      height: 300px;
      border-radius: 50%;
      background: radial-gradient(circle at center, #5f2c82, transparent);
      z-index: 0;
    }

    .bg-circle.top {
      top: -100px;
      right: 150px;
    }

    .bg-circle.bottom {
      bottom: -100px;
      left: 100px;
    }

    @media (max-width: 900px) {
      .container {
        flex-direction: column;
        justify-content: flex-start;
        align-items: center;
        height: auto;
        padding: 40px 10px;
      }
      .dashboard-section {
        margin-right: 0;
        margin-bottom: 30px;
      }
    }
  </style>
</head>
<body>
  <div class="bg-circle top"></div>
  <div class="bg-circle bottom"></div>
  <div class="container">
    <div class="dashboard-section welcome-section">
      <h1>
        <% if (role === 'freelancer') { %>
          Hi, <span><%= user.name %></span>!<br>
          <small>Welcome to your Freelancer Dashboard</small>
        <% } else if (role === 'recruiter') { %>
          Hi, <span><%= user.name %></span>!<br>
          <small>Welcome to your Recruiter Dashboard</small>
        <% } %>
      </h1>
      <p>
        <% if (role === 'freelancer') { %>
          <strong>About:</strong> <%= user.about %>
        <% } %>
      </p>
    </div>
    <div class="dashboard-card">
      <% if (role === 'freelancer') { %>
        <h2>Profile Summary</h2>
        <p>Name: <%= user.name %><br>Role: Freelancer</p>
        <h3>Jobs Applied To</h3>
        <ul class="dashboard-list">
          <% if (appliedJobs && appliedJobs.length > 0) { %>
            <% appliedJobs.forEach(function(job) { %>
              <li>
                <a class="dashboard-link" href="/jobs/<%= job.id %>"><%= job.title %></a>
              </li>
            <% }); %>
          <% } else { %>
            <li>No jobs applied yet.</li>
          <% } %>
        </ul>
        <a class="dashboard-btn" href="/appliedJobs">View All Applied Jobs</a>
        <a class="dashboard-btn" href="/jobs">Browse Jobs</a>
        <a class="dashboard-btn" href="/profile">Edit Profile</a>
        <form action="/logout" method="POST" style="display:inline;">
          <button type="submit" class="dashboard-btn logout-btn">Logout</button>
        </form>
      <% } else if (role === 'recruiter') { %>
        <h2>Posted Jobs</h2>
        <ul class="dashboard-list">
          <% if (postedJobs && postedJobs.length > 0) { %>
            <% postedJobs.forEach(function(job) { %>
              <li>
                <a class="dashboard-link" href="/job-applications/<%= job.id %>"><%= job.title %></a>
              </li>
            <% }); %>
          <% } else { %>
            <li>No jobs posted yet.</li>
          <% } %>
        </ul>
        <a class="dashboard-btn" href="/post-job">Post a New Job</a>
        <a class="dashboard-btn" href="/freelancers">Browse Freelancers</a>
        <a class="dashboard-btn" href="/profile">Edit Profile</a>
        <form action="/logout" method="POST" style="display:inline;">
          <button type="submit" class="dashboard-btn logout-btn">Logout</button>
        </form>
      <% } %>
    </div>
  </div>
</body>
</html> -->

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Dashboard</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      min-height: 100vh;
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(120deg, #6a3093 0%, #a044ff 100%);
      display: flex;
      align-items: center;
      justify-content: center;
    }
    .dashboard-card {
      background: rgba(255,255,255,0.13);
      border-radius: 24px;
      box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.25);
      backdrop-filter: blur(14px);
      -webkit-backdrop-filter: blur(14px);
      padding: 44px 36px 36px 36px;
      width: 100%;
      max-width: 420px;
      text-align: center;
      color: #fff;
      margin: 32px 0;
      animation: fadeIn 0.9s cubic-bezier(.39,.575,.56,1) both;
    }
    @keyframes fadeIn {
      0% { opacity: 0; transform: translateY(40px);}
      100% { opacity: 1; transform: translateY(0);}
    }
    .dashboard-card h2 {
      font-size: 2rem;
      margin-bottom: 18px;
      font-weight: 700;
      letter-spacing: 1px;
    }
    .dashboard-section {
      margin-bottom: 24px;
      text-align: left;
    }
    .dashboard-section strong {
      color: #fff;
      font-weight: 600;
    }
    .dashboard-list {
      list-style: none;
      padding: 0;
      margin: 0 0 18px 0;
    }
    .dashboard-list li {
      margin-bottom: 10px;
      color: #e0e0e0;
    }
    .dashboard-btn, .dashboard-link {
      display: block;
      width: 100%;
      margin: 12px 0 0 0;
      padding: 12px 0;
      border-radius: 10px;
      border: none;
      background: linear-gradient(to right, #00c6ff, #0072ff);
      color: white;
      font-size: 1.06em;
      font-weight: 600;
      text-decoration: none;
      cursor: pointer;
      transition: background 0.2s, box-shadow 0.2s;
      box-shadow: 0 2px 12px rgba(0,114,255,0.08);
    }
    .dashboard-btn.logout-btn {
      background: linear-gradient(to right, #e96443, #904e95);
      margin-top: 16px;
    }
    .dashboard-btn:hover, .dashboard-link:hover {
      background: linear-gradient(to right, #0072ff, #00c6ff);
      box-shadow: 0 4px 16px rgba(0,114,255,0.16);
    }
    @media (max-width: 600px) {
      .dashboard-card {
        padding: 28px 10px 22px 10px;
        max-width: 98vw;
      }
    }
  </style>
</head>
<body>
  <div class="dashboard-card">
    <h2>
      <% if (role === 'freelancer') { %>
        👋 Welcome <%= user.name %>!<br>
        <span style="font-size:1rem; font-weight:400;">You’re logged in as a <strong>Freelancer</strong>.</span>
      <% } else if (role === 'recruiter') { %>
        👋 Welcome <%= user.name %>!<br>
        <span style="font-size:1rem; font-weight:400;">You’re logged in as a <strong>Recruiter</strong>.</span>
      <% } %>
    </h2>

    <% if (role === 'freelancer') { %>
      <div class="dashboard-section">
        <strong>Profile:</strong><br>
        Name: <%= user.name %><br>
        About: <%= user.about || 'No bio yet.' %>
      </div>
      <div class="dashboard-section">
        <strong>Jobs Applied To:</strong>
        <ul class="dashboard-list">
          <% if (appliedJobs && appliedJobs.length > 0) { %>
            <% appliedJobs.forEach(function(job) { %>
              <li>
                <a class="dashboard-link" href="/jobs/<%= job.id %>"><%= job.title %></a>
              </li>
            <% }); %>
          <% } else { %>
            <li>No jobs applied yet.</li>
          <% } %>
        </ul>
      </div>
      <a class="dashboard-btn" href="/appliedJobs">View All Applied Jobs</a>
      <a class="dashboard-btn" href="/jobs">Browse Jobs</a>
      <a class="dashboard-btn" href="/profile">Edit Profile</a>
      <form action="/logout" method="GET" style="margin:0;">
        <button type="submit" class="dashboard-btn logout-btn">Logout</button>
      </form>
    <% } else if (role === 'recruiter') { %>
      <div class="dashboard-section">
        <strong>Posted Jobs:</strong>
        <ul class="dashboard-list">
          <% if (postedJobs && postedJobs.length > 0) { %>
            <% postedJobs.forEach(function(job) { %>
              <li>
                <a class="dashboard-link" href="/job-applications/<%= job.id %>"><%= job.title %></a>
              </li>
            <% }); %>
          <% } else { %>
            <li>No jobs posted yet.</li>
          <% } %>
        </ul>
      </div>
      <a class="dashboard-btn" href="/post-job">Post a New Job</a>
      <a class="dashboard-btn" href="/freelancers">Browse Freelancers</a>
      <a class="dashboard-btn" href="/profile">Edit Profile</a>
      <form action="/logout" method="GET" style="margin:0;">
        <button type="submit" class="dashboard-btn logout-btn">Logout</button>
      </form>
    <% } %>
  </div>
</body>
</html>
